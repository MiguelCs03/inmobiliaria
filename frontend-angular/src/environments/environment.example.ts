export const environment = {
  production: false,
  graphqlUri: 'http://localhost:3001/graphql', // Reemplace con la URL de su API Gateway en producción
  apiRestUri: 'http://localhost:3001/ia'       // Base URL para APIs REST de ML (Django)
};
